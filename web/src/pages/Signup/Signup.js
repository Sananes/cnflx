import React from "react";
import Layout from "../../components/Layout";
import styles from "./Signup.module.scss";

const SigninIndex = () => {
  return (
    <Layout customStyle={true} className={styles["signin"]}>
      <div>Hey there</div>
    </Layout>
  );
};

export default SigninIndex;
